version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - DATABASE_URL=file:./dev.db
    volumes:
      - ./prisma:/app/prisma
      - ./uploads:/app/uploads
    restart: always
